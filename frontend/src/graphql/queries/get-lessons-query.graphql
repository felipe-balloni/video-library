query GetLessons {
    lessons(sort: ["availableAt:asc"]) {
        data {
            id
            attributes {
                title
                slug
                description
                availableAt
                type
            }
        }
    }
}
